# App
NEXT_PUBLIC_APP_URL=https://90tasks.ki-katapult.de

# Auth
ALLOWED_EMAILS=tim.neunzig@gmail.com,tim.neunzig@posteo.de
API_KEY=your-api-key-for-clawdbot

# Email (Postal)
SMTP_HOST=postal.ki-katapult.de
SMTP_PORT=587
SMTP_USER=tasks@ki-katapult.de
SMTP_PASS=your-smtp-password
SMTP_FROM="90Tasks" <tasks@ki-katapult.de>

# Database
DATABASE_PATH=/app/data/tasks.db
